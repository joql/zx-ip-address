name: PHP Composer

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install Vendor Packages
        run: COMPOSER=composer-phpunit.json && composer install

      - name: Start Tests
        run: ./vendor/bin/phpunit
